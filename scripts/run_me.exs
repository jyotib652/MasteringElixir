    # 1. Call the function from your lib folder
    message = PracticeElixir.say_hello("Alice")

    # 2. Print the result to the terminal
    IO.puts(message)

    # 3. Do some simple math to test the environment
    sum = 10 + 32
    IO.puts("The answer to everything is #{sum}")
